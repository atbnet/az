apiVersion: v2
name: web
description: Web front-end on AKS, exposed via Application Gateway for Containers (Gateway API)
type: application
version: 0.1.0
appVersion: "0.1.0"
kubeVersion: ">=1.30.0-0"
